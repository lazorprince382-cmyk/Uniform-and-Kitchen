NODE_ENV=production
PORT=3002

# Same Postgres password as Uniform (use %23 for # in URL)
DATABASE_URL=postgresql://postgres:pri372%23nce@localhost:5432/kitchen_db

SESSION_SECRET=CHANGE_ME_LONG_RANDOM_STRING

# Must stay "/" (never /kitchen) — see server.js comments
COOKIE_PATH=/

# 10 minutes inactivity (milliseconds)
SESSION_MAX_AGE_MS=600000

# Uniform login page (port 8088) — client config only (public/config.js)
# KITCHEN_UNIFIED_LOGIN_URL=http://185.214.134.41:8088/login?system=kitchen

# After deploy, also set public/config.js:
#   window.KITCHEN_API_BASE = '/kitchen';
#   window.KITCHEN_BASE_PATH = '/kitchen/';
