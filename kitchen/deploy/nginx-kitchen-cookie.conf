# Kitchen location block (port 8088).
# Node app listens on /api/* — session cookie Path must be "/" (see server.js).
# Do NOT set COOKIE_PATH=/kitchen or proxy_cookie_path; that breaks login.
#
#   location ^~ /kitchen/ {
#       proxy_pass http://127.0.0.1:3002/;   # trailing slash required
#       proxy_http_version 1.1;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#   }
